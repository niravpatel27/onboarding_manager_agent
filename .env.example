# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Specify which OpenAI model to use
# OPENAI_MODEL=gpt-4

# Member Service API (if not using stubs)
MEMBER_SERVICE_API_URL=https://api.example.com/member-service
MEMBER_SERVICE_API_KEY=your-member-service-key

# Project Service API (if not using stubs)
PROJECT_SERVICE_API_URL=https://api.example.com/project-service
PROJECT_SERVICE_API_KEY=your-project-service-key

# Slack API (if not using stubs)
SLACK_API_TOKEN=xoxb-your-slack-bot-token

# Email Service (if not using stubs)
EMAIL_SERVICE_API_KEY=your-email-service-key

# Run Mode (local uses stubs, production uses real APIs)
RUN_MODE=local